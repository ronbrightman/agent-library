Frozen copy from `nextlevelbuilder/ui-ux-pro-max-skill`
(.claude/skills/ui-ux-pro-max/), MIT licensed. See LICENSE for the full
text. Fetched and copied verbatim on 2026-07-20.

**Scope note:** this skill's full upstream form is a searchable local
database (84 styles, 192 palettes, 74 font pairings, etc.) queried via
`scripts/search.py` against CSV data files. Those CSVs and the search
script were NOT copied (large generated data assets, low value to freeze
byte-for-byte, and this session's fetch access can't pull binary/large
repo trees). What was copied instead is the two reference docs that hold
the actual rule content in readable form
(`references/quick-reference.md`, `references/pro-rules.md`) plus the
SKILL.md. The `--design-system`/`search.py` CLI workflow described in
SKILL.md will not work as-is; use the reference docs directly instead.
